standby_mode = 'on'
primary_conninfo = 'user={{replicator}} password={{replicator_pass}} host={{master_host}} port={{master_port}} sslmode=prefer sslcompression=1'
trigger_file = 'trigger-failover'
restore_command = 'cp /var/backups/postgresql/archive/%f %p'
